# text_command_interface
A text command interface that selects buttons on a GUI.

Type the name of the buttons to change their color as single word commands only. Type reset or clear to reset the changes. This is strict command based and does not use natural language. 
